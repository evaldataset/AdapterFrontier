# AdapterFrontier dependencies — pinned to the versions in the per-cell
# provenance blocks (released ensemble_results/*.json metadata).
#
# Tested environment:
#   - Python 3.10.12
#   - CUDA 12.6 (driver-side; runtime via torch wheel)
#   - NVIDIA A6000 48GB (local v1.2 runs); RTX 3060 12GB cluster (v1.0/v1.1)

# Core PEFT/Transformers stack (pinned)
torch==2.7.1
transformers==5.3.0
peft==0.18.1
datasets==4.5.0
numpy==2.2.6
accelerate>=0.34.0,<2.0.0
safetensors>=0.4.5,<1.0.0
sentencepiece>=0.2.0
tokenizers>=0.20.0

# Inference / multi-LoRA serving
vllm==0.10.0
bitsandbytes>=0.45.0,<1.0.0   # 4-bit QLoRA (Llama-3.1-8B-Instruct cell only)

# Statistical analysis (compute_match.py + frontier regression)
scipy>=1.13.0,<2.0.0
scikit-learn>=1.5.0,<2.0.0

# Plotting (figure regeneration scripts in analysis/)
matplotlib>=3.9.0,<4.0.0

# Bibliography + LaTeX compilation are external requirements (texlive-full
# plus acl-style-files byte-identical to https://github.com/acl-org/acl-style-files
# at the version documented in paper/acl.sty).

# Optional: dataset download cache + manifest validation
huggingface-hub>=0.26.0,<1.0.0
pyyaml>=6.0
tiktoken>=0.6.0
