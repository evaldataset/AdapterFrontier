# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
venv/

# Generated training/eval artifacts (large, reproducible from configs+code)
sweep_runs/
cluster_runs/
collected_adapters/
ensemble_cache/
merged_adapters/
experiments/results/
*.npy
*.safetensors
*.bin

# Pool manifests and result JSONs are kept (small, paper-relevant)
# but uncomment below to exclude them from public mirror if needed:
# pools/
# ensemble_results/
# analysis/*.json

# Logs and monitoring
logs/
*.log

# Editor / OS
.vscode/
.idea/
.DS_Store

# Project memory / runtime state (oh-my-claudecode + similar)
.omc/
.claude/
.notepad.md

# Secrets — never commit
.env
*.secret
*.token
.hf_token
hf_token
hosts_*.csv.local

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.bbl
paper/*.blg
paper/_build/
